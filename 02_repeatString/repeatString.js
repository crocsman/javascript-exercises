const repeatString = function() {
    let count = 0;
    let repeated = "";
    while(count<10)
    {   
        repeated = repeated+"hey";
        count++;
    }   
    return repeated;
};

// Do not edit below this line
module.exports = repeatString;
